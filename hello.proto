syntax = "proto3";

package hello;

message Hello {
  string name = 1;
}

message HelloResult {
  string result = 1;
}

service HelloService {
  rpc Greet (Hello) returns (HelloResult) {}
  rpc Create (Hello) returns (HelloResult) {}
}